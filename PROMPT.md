# 最初に1回だけ貼るもの

今日は、git のコマンドを自分で打ちません。**手元の AI に打ってもらいます。**

---

## その前に: AI をどこで起動するか

**新しく空のフォルダを1つ作って、その中で AI を起動してください。**
デスクトップに `github-benkyokai` のような名前で作れば十分です。

いま作りかけのプロジェクトの中で起動すると、**今日作るリポジトリがその中に入れ子で
できてしまいます。** 空の場所から始めると、そこだけで完結します。

**今日は最後まで、そのフォルダから動きません。** AI が別の場所へ移動しようとしたら、
「いまのフォルダの中でやってください」と言ってください。

まず AI に、下のかたまりをそのまま貼ってください。**今日はこれを最初に1回だけ**です。

```
これから GitHub の練習をします。以降ずっと次を守ってください。

・実行した git / gh コマンドを、実行した順にそのまま見せる
・各コマンドが何をしたかを1行ずつ日本語で説明する
・最後に「手元」と「GitHub側」がいまどうなっているかを説明する
・私が次を頼むまで、勝手に先のステップに進まない
```

---

## 名前とメールを設定する（これも最初に1回）

続けて、これも貼ってください。**記録に残る「名前」と「メールアドレス」の設定**です。

```
git の user.name と user.email が設定されているか確認してください。

設定されていなければ、次のように設定してください。
・gh api user から id と login を取る
・user.email は「id+login@users.noreply.github.com」の形にする
・user.name は login をそのまま使う

そのあと、gh auth setup-git も実行してください。

最後に git config --global --list の結果を見せてください。
```

### `gh auth setup-git` も入れてある理由

`gh auth login` の途中に「**Authenticate Git with your GitHub credentials?**」という
質問があります。ここで No と答えた方や、`gh` を入れる前に `git` が無かった方は、
**あとで `git push` したときにユーザー名とパスワードを聞かれて止まります。**

`gh auth setup-git` はそれを設定し直すコマンドです。**すでに設定済みの方が実行しても
何も壊れません**ので、全員が打って構いません。

### なぜ要るのか

`gh auth login` が通しているのは **GitHub との通信**だけで、
**記録に残す名前とメールは別の設定**です。ここが空だと、こうなります。

- **Mac** … 記録はできますが、`名前@MacBook-Air.local` のような身に覚えのないアドレスで残り、
  **GitHub 上で自分のコミットとして表示されません**（アイコンもリンクも付きません）
- **Windows** … そもそも記録できずに止まることがあります

### なぜ自分のメールアドレスを使わないのか

GitHub が、**公開しても大丈夫な代わりのアドレス**を配っています。
`12345678+ユーザー名@users.noreply.github.com` という形のものです。

今日は途中から**みんなで同じリポジトリを触る**ので、
自分の本当のアドレスを設定すると、それが記録に残って参加者全員から見えます。
GitHub の設定によっては、`GH007` というエラーで弾かれることもあります。

上のプロンプトなら、**AI がこのアドレスを自分で組み立ててくれる**ので、
どこかを見に行く必要はありません。

---

## 「実行していいですか」と聞かれたら

AI は、コマンドを打つ前に**許可を求めてきます。**

```
Allow `gh repo create github-practice --private --add-readme --clone`?
```

こういう画面が出たら、**そこに書いてあるコマンドを一度読んでから許可してください。**

面倒に見えますが、**これが今日の練習そのものです。** 何が実行されるのかを見てから通す。
実際に AI にコードを書かせるときも、やっていることは同じです。

読んでもよく分からなかったら、許可する前に聞けます。

```
いま実行しようとしているコマンドは何をしますか？
```

### Codex を使っている方へ

Codex は、**一度失敗してから許可を求めてくることがあります。**
最初は制限された状態で実行しようとして弾かれ、そのあと「許可しますか」と聞いてくる、
という順番です。**エラーが出てから聞かれるのが正常**なので、驚かず許可してください。

---

## なぜコマンドを打たないのか

実際の仕事でコマンドを叩くのは、もう AI です。

なので今日覚えてほしいのは、**コマンドの綴りではなく「いま自分がどういう状態にいるか」の読み方**です。
AI が何をしたかを読めれば、あとは全部 AI に任せられます。

---

## 最後の1行がいちばん大事

```
・私が次を頼むまで、勝手に先のステップに進まない
```

これを入れないと、AI は気を利かせて**今日やることを全部いっぺんに終わらせてしまいます。**
1つずつ止まってもらわないと、何が起きたのか読む時間がありません。

途中で AI が先走ったら、こう言えば戻せます。

```
先に進みすぎです。いま何をやったかだけ説明して、そこで止まってください。
```

---

## AI が違うコマンドを使っても大丈夫

各ページに「**AI が打つはずのコマンド**」を載せています。
ただし AI によっては別の書き方をすることがあります（`git switch` の代わりに `git checkout` など）。

**やっていることが同じなら問題ありません。** 気になったら AI に聞いてください。

```
いま打ったコマンドは、docs に書いてあるものと同じことをしていますか？
```

---

[README に戻る](README.md)

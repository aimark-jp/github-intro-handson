# This file is a SAMPLE. It contains no real values, so it is safe to commit.
#
# The real file is named `.env` and it holds actual keys and passwords.
# `.env` is listed in .gitignore, so it never gets uploaded to GitHub.
#
# Rule of thumb: commit `.env.example`, never commit `.env`.

API_KEY=your-api-key-here
DATABASE_PASSWORD=your-password-here
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
